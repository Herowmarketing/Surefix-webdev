# Dependencies (Vercel installs these during the remote build)
node_modules

# Build output (regenerated by the build command)
dist

# Sanity Studio — deployed separately, not part of the marketing site
studio-sure-fix-remodeling

# Large stray working video files in the repo root (NOT used by the site;
# the served hero video lives in public/Sure Fix Hero Video/). These exceed
# Vercel's 100 MB per-file upload limit.
/Hero video Upscaled.mp4
/Sure Fix Hero Video Final.mp4
